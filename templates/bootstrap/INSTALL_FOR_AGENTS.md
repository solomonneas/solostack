# Install For Agents

## Start Here
1. Read `AGENTS.md`.
2. Read `TOOLS.md` for local commands.
3. Check `MEMORY.md` for durable pointers.
4. Inspect repo-local instructions before editing any project.

## Verification

```bash
git status --short
find . -maxdepth 2 -name AGENTS.md -o -name CLAUDE.md
```

Report what you changed and what verification ran.
